import { createInterFont } from '@tamagui/font-inter';

// NOTE: https://github.com/tamagui/starter-free/blob/main/packages/config/src/tamagui.config.ts
// NOTE: https://github.com/status-im/nimbus-gui/blob/main/tamagui.config.ts
// NOTE: https://github.com/Galaxies-dev/moviestar-stream/blob/main/tamagui.config.ts

export const pretendardThin = createInterFont({
  family: 'PretendardThin',
  face: {
    100: { normal: 'PretendardThin' },
  },
});

export const pretendardExtraLight = createInterFont({
  family: 'PretendardExtraLight',
  face: {
    200: { normal: 'PretendardExtraLight' },
  },
});

export const pretendardLight = createInterFont({
  family: 'PretendardLight',
  face: {
    300: { normal: 'PretendardLight' },
  },
});

export const pretendardRegular = createInterFont({
  family: 'PretendardRegular',
  face: {
    400: { normal: 'PretendardRegular' },
  },
});

export const pretendardMedium = createInterFont({
  family: 'PretendardMedium',
  face: {
    500: { normal: 'PretendardMedium' },
  },
});

export const pretendardSemiBold = createInterFont({
  family: 'PretendardSemiBold',
  face: {
    600: { normal: 'PretendardSemiBold' },
  },
});

export const pretendardBold = createInterFont({
  family: 'PretendardBold',
  face: {
    700: { normal: 'PretendardBold' },
  },
});

export const pretendardExtraBold = createInterFont({
  family: 'PretendardExtraBold',
  face: {
    800: { normal: 'PretendardExtraBold' },
  },
});

export const pretendardBlack = createInterFont({
  family: 'PretendardBlack',
  face: {
    900: { normal: 'PretendardBlack' },
  },
});
